/* E4 — Seconds to hours, minutes, seconds.                   [graded]
   GIVEN   : one integer: a number of seconds, 0..86399
   ENSURES : final output line ENDS WITH exactly:  <H> h <M> m <S> s
   EXAMPLE : input "7384"  ->  final line  "2 h 3 m 4 s"
   TOOLS   : / and % are all you need (Week 2's quotient & remainder!)
   CHECK   : ./check.sh e4                                              */
#include <stdio.h>

int main(void) {
    /* your program here */
    return 0;
}
