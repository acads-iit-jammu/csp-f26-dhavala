/* E2 — Area and circumference of a circle.                   [graded]
   GIVEN   : the radius, a decimal 0.5..20.0 — STORE IT IN A float
   REQUIRES: pi must be a NAMED CONSTANT, exactly 3.14159
             (use  #define PI 3.14159   or a const — the checker looks!)
   ENSURES : final output line ENDS WITH exactly:  area=<A> circ=<C>
             each with exactly 3 decimal places (%.3f)
   EXAMPLE : input "2.0"  ->  final line  "area=12.566 circ=12.566"
   CHECK   : ./check.sh e2                                              */
#include <stdio.h>

int main(void) {
    /* your program here */
    return 0;
}
