/* E3 — Quotient, remainder, and the decimal quotient.        [graded]
   GIVEN   : two integers a b on one line, each 1..100
   ENSURES : final output line ENDS WITH exactly:  q=<Q> r=<R> qf=<F>
             Q = integer quotient, R = remainder, F = decimal quotient
             with exactly 2 decimal places (%.2f)
   EXAMPLE : input "17 5"  ->  final line  "q=3 r=2 qf=3.40"
   THINK   : why do q and qf differ? Say it in a comment in this file.
   CHECK   : ./check.sh e3                                              */
#include <stdio.h>

int main(void) {
    /* your program here */
    return 0;
}
